import { FC } from 'react';
import { StyledNav } from './footer.styles';

const Footer: FC = () => {
  return (
    <StyledNav>
      <a href="#">Support</a>
      <a href="#">How it Works</a>
      <a href="#">Reviews</a>
    </StyledNav>
  );
};

export default Footer;
